package bdma.ulb.tpcdi.util

import static java.util.Objects.isNull

class Numbers {
    private static final double ZERO = 0.0
    static Double normalize(Double number) {
        if(isNull(number)) {
           return ZERO
        }
        return ZERO
    }
}
